name: authentication
description: "A new Flutter project."
publish_to: 'none' 
version: 1.0.0+1

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  flutter_tts: ^3.1.0  # Correct dependency for text-to-speech
  speech_to_text: ^6.6.0
  dialogflow_flutter: ^1.0.0
  vibration: ^1.7.3
  local_auth: ^2.1.3
  flutter_vibrate: ^1.3.0
  audioplayers: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  flutter_svg: ^0.22.0
flutter:
  uses-material-design: true
